# Merge3

A TypeScript project with unit tests for merging three sorted arrays.

## Setup Dependencies

Install all required dependencies:

```bash
npm install
```

## Execute Code

To run the TypeScript code:

```bash
npx ts-node src/merge.ts
```

Or compile and run:

```bash
npx tsc
node dist/merge.js
```

## Execute Unit Tests

Run the unit tests:

```bash
npm test
```
